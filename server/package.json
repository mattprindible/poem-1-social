{
  "name": "mfd-hub",
  "version": "0.0.0",
  "private": true,
  "description": "A SAN hub: one owner's identity, devices, and app library.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types env.d.ts --include-runtime false",
    "typecheck": "tsc --noEmit",
    "gen-key": "node scripts/gen-key.mjs"
  },
  "dependencies": {
    "@atproto-labs/did-resolver": "^0.3.5",
    "@atproto-labs/handle-resolver": "^0.4.6",
    "@atproto/jwk-jose": "^0.2.4",
    "@atproto/jwk-webcrypto": "^0.3.4",
    "@atproto/oauth-client": "^0.7.11",
    "@inanimate/resident": "^0.1.0",
    "agents": "^0.10.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^6.0.2",
    "wrangler": "^4.81.1"
  }
}
