{
  "name": "poem1-hub",
  "version": "0.0.0",
  "private": true,
  "description": "Poem/1 Social hub: the Resident relay for one person's device.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types env.d.ts --include-runtime false",
    "typecheck": "tsc --noEmit",
    "gen-key": "node scripts/gen-key.mjs"
  },
  "dependencies": {
    "@inanimate/resident": "^0.1.0",
    "agents": "^0.10.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^6.0.2",
    "wrangler": "^4.81.1"
  }
}
